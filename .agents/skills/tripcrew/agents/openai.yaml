interface:
  display_name: "Tripcrew"
  short_description: "Repo-specific patterns and workflows for tripcrew"
  default_prompt: "Use the tripcrew repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true