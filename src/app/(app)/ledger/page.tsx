import { SectionHeader } from "@/components/layout/SectionHeader";

export default function LedgerPage() {
  return (
    <section className="py-14 pb-24 section-enter">
      <SectionHeader
        code="§ 05"
        title="Ledger."
        lead="Pool everything, split even. Log what you pay, balances update. Settle on the flight home."
      />
    </section>
  );
}
